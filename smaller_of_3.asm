
	AREA RESET, CODE
	ENTRY
	
	MOV R0, #3
	MOV R1, #12
	MOV R2, #10
	
	;STORE MIN
	MOV R3, #0
	CMP R0, R1
	;IF A < B THEN CARRY CLEAR
	BCC AC
	BCS BC
	
AC

	;IF A < C
	CMP R0, R2
	MOVCC R3, R0
	BCC DONE
	BCS BC

BC
	CMP R1, R2
	MOVCC R3, R1
	MOVCS R3, R2
	B DONE

DONE
STOP B STOP
	
	END
